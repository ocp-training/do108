apiVersion: v1
kind: Secret
metadata:
  name: fids-db-credentials
type: Opaque
stringData:                 # stringData takes plain text; the API encodes it
  POSTGRESQL_USER: fids
  POSTGRESQL_PASSWORD: fids-training
  POSTGRESQL_DATABASE: flights